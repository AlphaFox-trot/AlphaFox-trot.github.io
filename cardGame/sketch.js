// Seige on fort stone
// Your Name
// Date

let turn, allyCards = [], allyhand = [], enemyCards = [], enemyHand = [];
let view;

function setup() {
  createCanvas(windowWidth, windowHeight);
}

function draw() {
  background(220);
}
